def firstMissingPositive(nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        #N = len(nums)
        #if N==0:
        #        return 1
        #for cursor in range(N): 
        #    target = nums[cursor]
        #    while target <= N and target>0 and target != nums[target-1]:
        #        new_target = nums[target-1]
        #        nums[target-1] = target
        #        target = new_target
        #        print(nums)
        #Pass 2, find first location where the index doesn't match the value
        #for cursor in range(0,N):
        #    if nums[cursor] != cursor+1:
        #        return cursor+1
        #return N+1

        d


a = firstMissingPositive([2,1])
print(a)
    
